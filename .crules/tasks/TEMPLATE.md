---
id: TASK-000            # unique, incrementing; filename must be <id>-<slug>.md
title: <one line>
owner: unclaimed        # agent name; MUST be set before any code is touched
status: wip             # wip | review | done (must match containing directory)
branch: <type>/<slug>   # feat/, fix/, docs/, chore/, refactor/
worktree: <path or n/a> # one task = one worktree when parallel work is possible
created: YYYY-MM-DD
---

## Scope
What is in and out of this task. Explicit non-goals.

## Success criteria
Verifiable checks (commands, expected output, file state). "Done" is falsifiable.

## Log
- YYYY-MM-DD <agent>: claimed / progress note / handoff
