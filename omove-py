#!/usr/bin/env bash
# Thin launcher for the Python omove CLI (minimal / pre-pipx phase).
set -euo pipefail
ROOT="$(cd "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
export PYTHONPATH="${ROOT}/src${PYTHONPATH:+:${PYTHONPATH}}"
exec python3 -m omove "$@"
